# Reload config on change
bind r source-file ~/.config/tmux/tmux.conf \; display "Reloaded"

# Set prefix to Ctrl-Space
unbind C-Space
set -g prefix C-Space
bind C-u send-prefix

## ZSH as default shell
set -g default-shell /bin/zsh

## Enable 256 and true color support
set -g default-terminal "screen-256color"
set -ga terminal-overrides ",*256col*:Tc" # Override to enable true-color for compatible terminals

## Mouse support
set -g mouse on

# Mouse select -> OS clipboard
#set -g @yank_selection_mouse 'clipboard'

# Handled by tmux-yank plugin
# Enter C-Space v to enter copy mode
# Enter 'Y' to yank selected text -> Text is placed into system 
# primary clipboard as well as local clipboard
# Enter 'P' to paste text inside of TMUX

## Increase history
set-option -g history-limit 32767

set -g set-clipboard on
set -g escape-time 0 
set -g status-interval 2
set -g allow-passthrough on
set -g status-position top

## Set | for vertical split
bind-key "|" split-window -hc "#{pane_current_path}"

## Set - for horizontal split
bind-key "-" split-window -vc "#{pane_current_path}"

# Use vim arrow keys to resize
bind -r j resize-pane -D 5
bind -r k resize-pane -U 5
bind -r l resize-pane -R 5
bind -r h resize-pane -L 5

# Use m key to maximize pane
bind -r m resize-pane -Z

# Start windows and panes at 1, not 0
set -g base-index 1
set -g pane-base-index 1
set -g renumber-windows on # Automatically renumber windows when one is closed

# Load theme
run-shell "tmux source-file ~/.config/tmux/default.theme"

# Add plugins
set -g @plugin 'plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-yank'

# RUN TPM
run '~/.config/tmux/plugins/tpm/tpm'

